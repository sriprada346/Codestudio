class Solution {
    public int maxProduct(int[] nums) {
         int len = nums.length;
        if(len==2)
            return (nums[0]-1)*(nums[1]-1);
        else
        {
            Arrays.sort(nums);
            return (nums[len-1]-1)*(nums[len-2]-1);
        }
    }
}
